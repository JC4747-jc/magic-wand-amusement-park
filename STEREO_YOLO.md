# 主项目：YOLO + 双目定位

## 三帧视觉平均跟随 1.10.0（当前，以此节为准）

`BridgeStereoFusion` 默认使用 `StereoYoloLocator.followMode = VisualAverage`。初次稳定标定、右手点赞召唤、底部/高度配准和施法效果沿用原流程；召唤后用 YOLO 检测框及对应双目图像计算世界位置。只跟随三维平移，保留首次标定尺寸与召唤朝向。

每次可靠视觉测量按拍摄时间入队，最近三次测量由旧到新使用 0.2 / 0.3 / 0.5 权重；不足三次时使用对应最新权重并归一化。只保留最近 0.3 秒内的测量，拒绝超过 0.3 秒的返回、重复或倒序拍摄时间。平均后的 3 mm 死区抑制静止噪声。锚点直接采用滤波结果，不再叠加 AnchorManager 的指数平滑。

先执行 YOLO 置信度、双目质量/匹配数、边界及标定尺寸比例检查，再执行原有大于 6 cm 跳变的连续三次确认，最后求平均。确认新位置时清空旧平均窗口，避免被旧位置拉回。距离上一次接受的拍摄时间超过 0.3 秒后，重新要求三次连续位置证据；只有异常候选或漏检时保持最后位置。质量检查沿用较宽的物体可靠性门限：置信度至少 0.5、双目质量至少 0.04、至少 5 个匹配点，宽度为标定的 0.6～1.5 倍，高度为 0.65～1.35 倍，框离图像边界至少约 4 像素。

视觉模式下左手不驱动锚点，BeforeRender 不执行手部补偿，右手靠近不暂停视觉；遮挡导致检测或深度不可靠时保持最后位置。Inspector 可切换 `HybridGrip` 恢复 1.9.5 的手部接续与右手视觉保护；运行时切换清除旧测量和手部状态，再等待新的视觉证据。

面板显示 `VISUAL FOLLOW 1.10.0 (3 frames)`，`FollowMetrics` 增加 mode 和 averageSamples，成功视觉位置路径为 `StereoVisualAverage3`。编辑器检查 `VisualAverageChecks` 覆盖三轴加权平均、重复/乱序/过期数据、跳点、漏检后重获、静止噪声，以及实际双目投影到显示锚点、右手保护旁路、手部不接管和模式切换。属于合成回归，不代表头显实测精度。

实机验证：让直立实物静止完成标定 → 右手点赞召唤 → 缓慢左右/上下/前后移动 → 停止观察抖动 → 短暂遮挡观察模型保持 → 重新露出观察恢复。完全遮挡时不会继续追踪移动，不估计实物旋转。本版不改 YOLO 权重；若频繁 NO_LIGHTER 优先补充实机移动和手持遮挡训练数据，若框稳定但深度跳动则先排查双目。

## 遮挡时夹持接管与连续视觉移动 1.9.5（历史；HybridGrip 模式）

1.9.4 实机在 00:50:30～00:51:30 多次进入抓握候选，却没有一次 CONFIRMED；画面存在大量 NO_LIGHTER/LighterTooSmall。之后大量 CONFIRMING_MOVE 表明视觉防跳变也阻止了移动目标，两个路径同时未能有效接管。

本版增加几何夹持路径：以已配准的真实物体位置、宽度和高度为参考，要求拇指和食指处于机身两侧、两指连线穿过机身附近、间距合理，并短暂稳定。夹持成立后可直接启用手部接续，不再必须等待被手遮挡后的 YOLO 同动证据。空捏到指尖相触、同侧捏合、机身上方捏合不能通过。候选期间沿用持有的松开阈值，避免未确认阶段手型反复退出。原有视觉同动确认及“实物停住而手离开”的解除跟随路径保留。

视觉跳变确认改为比较相邻采样：允许有界速度、方向连续的移动轨迹连续三帧通过，不再要求移动物体停在第一次候选点的小范围中；独立乱跳点仍被拒绝。合成检查涵盖这两条路径和最终显示锚点，不能代表实机物体真值。夹持几何是接触估计，不是接触传感器：手部或初始实物定位误差仍可能导致误判，完全遮挡且不张指的放下仍需要重新露出机身完成视觉解除。

版本面板 LIGHTER GRIP 1.9.5，新增 CONTACT_CONFIRMED 日志及 FollowMetrics.contact。验证顺序：空捏 → 两指实际夹住拿起 → 放下张开并移手 → 放下保持手型移手且露出机身。花朵不变。

## 真实拿起与放下判定 1.9.4（历史）

修正仅靠左手靠近并捏合就接管模型的问题。PinchGripFollow 现在只是候选姿态估计，LighterHoldEvidence 才决定是否允许驱动最终锚点。抓握候选期间，模型继续使用视觉定位；需要两个有足够时间间隔的可靠 YOLO+双目样本，确认物体与拍摄时刻的手部预测一起移动至少 18 mm、方向相符、位移差小于 25 mm，才转为持物接续。空手捏指、物体独立移动不能授予手部跟随权限。渲染前补偿同样检查已确认的持物权限。

确认拿起后，视觉暂时丢失不再冻结有效手持跟随。持续监测独立的实物测量：如果实物连续三帧停在同一位置而手离开，则解除手部接管，将实物位置交回视觉锚点，随后空手不会因仍保持捏合而重新带走模型。放下不限定原来的桌面或高度，也支持另一处桌面。张指、手部追踪丢失仍会退出；有效张指不会被指尖跳变过滤误当成遮挡。

状态：VERIFY PICKUP 表示尚未确认拿起，FOLLOW LEFT GRIP 表示已确认。日志新增 [ObjectGrip] CANDIDATE / CONFIRMED / RELEASED，以及 FollowMetrics.ownership。测试覆盖空手、真实拿起、拿起后遮挡、放下后捏指离开、不重新误抓取，以及最终渲染锚点的权限检查。仍依赖视觉能识别真实机身；拿起确认和放下确认时尽量露出机身，完全遮挡下无法仅凭手势可靠区分“拿着”与“空捏”，不能保证任意遮挡条件下识别真实接触。

花朵沿用 1.9.3 的机身覆盖尺寸与整体比例。

## 持物跟随回归修复与覆盖花朵 1.9.3（历史）

1.9.2 的实机日志在 17:51:13 成功 ATTACHED，之后 handGapMs 约 12 ms，说明手部持续更新；但外观匹配分数约 0.41～0.58，没有通过门限。新增的 ObjectRecent 0.9 秒硬门限把手部位置写入和 BeforeRender 都拦住了，因此模型固定。这是回归，不能把内部估计在移动当作最终模型在跟随。

1.9.3 取消视觉新鲜度对手持移动的硬门限：左手保持有效握持就持续更新锚点，右手施法和 YOLO 漏检都不切断左手跟随。灰度模板仅用于额外视觉纠偏，不再一票否决正常 YOLO 检测；尺寸、时间对齐和纠偏连续性检查仍保留。右手视觉保护距离从 30 cm 收至 18 cm，离开后延迟从 0.8 秒收至 0.45 秒，减少大范围停用视觉。手指弯曲不直接改变已捕获的持物偏移；手部真正丢失、松手仍退出持有。面板 FOLLOW LEFT GRIP - vision pending 表示手持跟随继续、正在等待视觉纠偏，不是冻结。

花朵恢复完整覆盖：直花茎直径为已配准模型水平包围盒对角线加 4 mm，高度超过机身 6 mm；花头直径至少为花茎的 4.5 倍，叶片随花茎比例调整，底部沿用真实锚点。保持原有粉色层叠花瓣和原变花手势。覆盖取决于已有定位贴合，不能补偿定位偏移或真实打火机大幅倾斜。

新增 NO_VISION_RENDERED_FOLLOW_CHECK_PASS 检查实际 Locator → AnchorManager → 显示锚点链路，在没有视觉模板、NO_LIGHTER、右手保护同时存在时仍跟随并执行 BeforeRender 补偿。花朵检查覆盖旋转一周中 16 个朝向的机身八角点，并检查花头/花茎比例。构建和合成检查不能替代 PICO 持物实测。

## 物体约束跟随与花朵比例修复 1.9.2（历史，冻结条件已在 1.9.3 移除）

实机日志确认多次 Rule 横移，以及持有期间大量 NO_LIGHTER、QUALITY_GATE，视觉纠偏没有稳定建立。本版横挥要求沿动作开始时的相机左右方向，向前伸手不再满足横挥条件；躲闪改为约半秒的横移并归位，切换到碳化/变花或 Reset 时取消残留位移。右手视觉保护同时覆盖真实锚点和虚拟模型周围，包含掌部、腕部及五个指尖，提前于左手更新执行。

初次成功标定后固定保存打火机灰度外观模板，不从后续遮挡画面自我更新。左手持有时，在手部预测区域附近搜索多个尺度的模板，以至少两个有纹理区域的相关性确认目标，再用双目深度验证；这一过程可在 YOLO 漏检时继续运行。YOLO 后续候选也须通过外观检查，降低把手指当成打火机更新位置的风险。手指只负责确认握持和初次偏移，保持握持后不再因指尖相对掌部的弯曲直接拖动目标；物体外观＋双目测量修正握持偏移。若超过约 0.9 秒没有可靠物体测量，停止继续写入手部推算位置，显示 HOLD - expose real lighter；重新看到目标后尝试恢复。

此模板方案适合当前有纹理、朝向变化小的打火机，不能保证完全遮挡、明显转向或纹理不足时仍贴合。新增 [ObjectTrack] CORRECTED 日志和 FollowMetrics 中的 object/score/objectAgoMs。两指捏持时先保持目标可见、移开右手，看到 FOLLOW OBJECT 后再移动。

花朵保留层叠花瓣，恢复正常细茎与连接自然的叶片；整体约 10～12 cm，取消按打火机宽度膨胀花茎。正常花茎不会完全遮住真实打火机，实际背景透视仍会露出部分实物。

## 跟随优化 1.9.1（当前）

本次仅优化已有跟随。诊断日志 `[FollowMetrics]` 每两秒记录手部更新间隔、滤波前后位置差、图像拍摄到返回的帧龄及最近响应距离现在的时间、时间匹配后的视觉/手部位置差、纠偏状态、接管次数、掌部接续次数和退出原因。filterGapMm 是算法内部位置差，visionResidualMm 是两种估计之间的差值，都不代表已知真实位置误差；-1 表示没有可用测量。日志仅在完成目标标定后输出，最后一次视觉指标需结合 replyAgoMs 判断是否仍新鲜。

接管时从当前显示位置开始，不瞬间跳到标定平均值。指尖暂时缺失但掌部仍可靠时接续最长 0.45 秒；恢复指尖时以短暂位置偏移衰减衔接，恢复窗口暂停渲染前补偿和视觉纠偏。完全没有有效手部更新仍在 0.35 秒后退出。张指释放仍为 0.18 秒，放下后第一批视觉定位必须连续三次一致才接管；右手遮挡保护继续生效。

防抖使用平滑后的速度调节跟随强度，降低静止关节噪声被误判为移动的影响；渲染前的小位移在静止时也会衰减。视觉死区从 6 mm 收至 3 mm，较大视觉位置差提高锚点追赶速度。自动化对比使用固定 100 Hz、±1 mm 交替噪声、0.25 m/s 平移的合成输入，结果保存在 .codex-tmp/follow-filter-benchmark.txt；这不是 PICO 实测精度或端到端延迟。

实机检查顺序：静止捏持十秒 → 缓慢平移十秒 → 轻微转腕 → 遮挡指尖约 0.3 秒后露出 → 张指放下再拿起。对照 `[FollowMetrics]` 判断固定偏差、移动落后、短暂恢复跳动分别来自哪个环节，再决定是否继续调参。

## 捏持跟随 1.9.0（当前交互，以此节为准）

左手拇指、食指捏住打火机，两指间距小于 5.5 cm、两指中点靠近已标定目标（8.5 cm 内），至少三个样本且持续 0.15 秒后接管。连续样本平均记录目标相对于捏持点的偏移；使用掌部旋转定义参考坐标，因此轻微转腕会改变位置偏移，但虚拟模型自身不旋转。持有时两指间距放宽至 7 cm，分开持续 0.18 秒释放。此版本优先支持两指捏持，不再用握拳手型触发左手接管。

动态更新负责抓握状态、变速防抖及手部历史记录；BeforeRender 只补偿最新手部位置差，不再次触发手势。指尖短暂缺失时使用最近标定的掌部关系接续，最多 0.25 秒；追踪完全丢失时保留最后位置并退出持有。

持有时继续发送视觉帧。只有较高 YOLO 置信度、足够双目匹配点、识别框尺寸接近首次标定、未触及画面边界，且右手不在目标附近施法时，才尝试纠偏。用拍摄时刻的手部历史插值匹配视觉位置；拒绝超过 0.35 秒、跨抓握阶段、过大偏差或不连续证据。连续三帧局部偏移一致后缓慢更新，每次修正最多 2 mm。此策略不能保证识别所有遮挡，且会主动拒绝不确定测量；画面中 Visual corrections 停在零也可能只是没有满足纠偏条件。

面板 FOLLOW LEFT PINCH 表示捏持跟随，PINCH - brief palm fallback 表示短暂掌部接续，Visual corrections 显示接受的纠偏次数。日志 [PinchFollow] ATTACHED / VISUAL_CORRECTION。验证：底部与中部分别捏持 → 缓慢平移 → 轻微转腕 → 短暂遮挡指尖 → 露出目标且右手离开检查纠偏 → 张开两指放下 → 重复召唤、碳化和变花，确认跟随与效果仍兼容。仍需最初视觉标定；明显倾斜、完全遮挡时滑动或大幅换握不能保证贴合。

## 加粗花茎与打火机外观 1.8.2（当前）

原有变花效果的花瓣造型保留，常规整体高度调整为 12～15 cm（按打火机高度 1.6 倍）。花茎为圆形粗茎，直径按已配准虚拟打火机水平包围盒对角线加 6 mm 计算；茎高至少超过打火机 8 mm，尺寸较大的目标优先保证覆盖，必要时抬高花头。Base 继续贴底，左手跟随和施法规则不变。覆盖以当前配准为基础，不能消除真实定位误差。

构建场景中的 virtual lighter 更换为圆角塑料外壳、金属防风罩、齿纹打火轮、按压片及通气孔细节，采用 URP Lit 塑料和金属材质。维持原有 Lighter 根节点、手势组件和底部/高度配准。主构建菜单会更新生成场景中的外观；BridgeTest 仍保留旧造型。实际预览：.codex-tmp/compact-flower-preview.png 和 .codex-tmp/realistic-lighter-preview.png。

## 紧凑花朵替换 1.8.1

替换原有 Flower.prefab，沿用“握拳后张开”的变花操作。三层 24 片珊瑚粉渐变花瓣、金色花蕊、短弯茎和三片绿叶，使用预生成网格和支持 XR 双眼实例化的顶点色材质。整体高度按变花时打火机高度的 1.2 倍缩放，限制在 7.5～10.5 cm；Base 子节点准确对齐打火机底部。继承原有手持跟随、旋转和恢复逻辑，金色粒子缩小并减少数量。模型生成器位于 Assets/Editor/FlowerModelBuilder.cs，实际 Unity 渲染预览为 .codex-tmp/compact-flower-preview.png。

## 手持位置跟随 1.8.0（当前行为，以此节为准）

左手拿真实打火机、右手召唤和施法。只跟随平移，保留召唤时朝向和首次标定尺寸，不做手部遮挡显示。首次让直立打火机充分可见并短暂停稳完成双目标定，不要求放在桌面上。右手点赞召唤后先张手，再进行原有施法动作。

左手掌心或拇指/食指尖距离目标不超过 10 cm，捏合或中指和无名指弯曲持续约 0.15 秒，便捕获掌心到目标的位置偏移。面板显示 `FOLLOW LEFT HAND` 后可以平移打火机；即使手挡住 YOLO，仍通过左手位置接续跟随。此时暂停视觉位置写入，左手捏合不会触发 Reset。右手施法不切断左手跟随。

左手张开时立即停止跟随张手的位移，持续约 0.18 秒后释放；手部追踪短暂丢失保留位置，超过 0.35 秒退出手持跟随。之后等待新的视觉测量接管，右手施法遮挡保护仍然生效。视觉跟随取消桌面高度约束，可以上下、左右、前后移动；大幅视觉跳点仍需多帧确认。重新握持需要靠近当前目标位置。

花朵及变花等待期间的生成位置随锚点平移。躲闪仍有故意的侧向动画偏移。此版本根据“附近抓握手型”推断拿起，不能确认真实接触；空手在目标附近握拳也可能接管。手内滑动、重新换握、转动造成的相对位置变化，以及手和目标同时完全不可见，不保证贴合。首次标定偏差不会被手持跟随自动消除。

验证：标定并召唤 → 左手靠近握住，确认 `FOLLOW LEFT HAND` → 保持朝向缓慢上下/左右/前后移动 → 用右手碳化、变花，移动左手检查花朵跟随 → 放下后张开左手并移开双手，确认视觉恢复 → Reset 后重新标定。日志新增 `[Handheld] LEFT_GRIP` 与 `[Handheld] RELEASE_OR_TRACKING_LOST`。编辑器检查新增 `HANDHELD_FOLLOW_CHECKS_PASS`。

## 桌面施法版 1.2.0

### 手势修复版 1.3.0

1.3.2 增加直立打火机的模型配准：以双目测得的中心位置为基准，将识别框上下边缘射线与经过该中心的竖直平面相交，估计底部和高度。模型按全部实体网格的高度等比缩放，再将实体底部对齐测量底部；不再直接将模型原点当作识别框中心。锁定前多帧平均高度和底部偏移。Reset 和生命缩放使用配准后的尺寸，花朵继续对齐实体底部。适用于桌面直立目标，不代表解决所有双目深度或相机外参误差；侧向移动头显仍需实机验证。日志 `[Registration]` 记录 bbox、高度、底部、相机位置和最终模型偏移。

1.3.1 修正花朵高度：解构时以虚拟打火机实体网格的底部中心为支撑点，将花茎底端对齐；取消原先 2 cm 抬高和 1.5 cm 上下浮动，保留绕支撑点的缓慢旋转与金色粒子。此修复对齐虚拟模型的底部，不额外估计真实桌面高度。

桌面场景使用 `TabletopGestureRecognizer` 统一识别，禁用旧的四个并行检测器。闭合的四指优先进入握拳状态，保持至少 0.12 秒，再在开始松拳后 0.7 秒内张开；握拳保持时间不再算进张开的时间窗口。捏合只在非握拳状态下识别，拇指食指小于 3.5 cm 后分开至 6 cm，持续 0.04～0.85 秒触发外观。每次识别后留出 1 秒反馈时间。

画圈要伸出食指、收起至少另外两指，完成直径约 5～16 cm 的闭合圆（0.5～3 秒，支持任意平面）；直线、往返和未闭合的弧不算圆。挥动要张开手掌，水平移动至少 10 cm，约 0.1～0.45 秒内完成。准备握拳、捏合或指向画圈时不累计挥动。

距离取掌心、食指尖、捏合中心到真实锚点的最近值。先进入 20 cm 范围，张手瞬间可在 32 cm 范围内保留 0.65 秒；超过外圈、丢失手部追踪或复位都会限制后续触发。不能直接站在 32 cm 外圈开始施法。面板显示实时手距、识别状态和最近的 Applied/Blocked 结果。旧版下文的 15 cm/0.55 秒规则不适用于 1.3。

日志：`[TabletopHand]`（手型、距离）、`[TabletopGesture]`（识别结果）、`[GestureTargetGate]`（目标许可）。

双目位置至少有五次测量、持续至少 0.6 秒，且位于首个候选点 4 cm 内时锁定；超过 0.8 秒的测量间隔会重新累计。锁定前隐藏模型并拒绝手势。面板出现 `READY - right hand to cast` 后停止发送识别帧，保存世界锚点；手遮挡、YOLO 丢失或电脑断开不再撤掉虚拟效果。此时帧编号停止增加是正常行为。面板的距离和深度为锁定时的测量值。

真实打火机需保持在桌面上。锁定后不会跟随真实打火机的移动；移动它后必须复位。头部追踪丢失或应用暂停会清除锁定与效果并重新定位。

构建菜单自动开启四种变换，手势以真实目标锚点为中心检查右手食指的实时世界位置（15 cm 内），不跟随躲闪后的虚拟模型位置。右手快速捏合并在 0.5 秒内松开：烧焦与烟雾；食指画小圈：眼睛和呼吸；快速水平挥动：侧向躲闪与残影；握拳后在 0.55 秒内张开：解构并生成花朵。每次动作间隔至少 0.5 秒。

左下方 `Reset` 按钮：左手在按钮 10 cm 内捏合保持 0.3 秒。复位清除花朵和模型变化，重新寻找真实打火机；右手不能触发此按钮。复位前把手移开目标，复位后等待 READY 再施法。

验收顺序：稳定目标出现 READY → 遮挡超过两秒确认模型保持 → 四种手势分别观察效果 → 躲闪后在原真实位置继续施法 → 解构后遮挡/移开手确认不会被视觉恢复 → 左手 Reset 清除效果 → 移动真实物体并重新锁定。相机和手势的实机精度需要戴头显验证。

这一版直接在 `magic-wand-amusement-park` Unity 项目内开发，把隔离项目验证过的双目取帧和相机坐标转换接入原来的 `DetectionManager → SingleTargetManager → AnchorManager → LighterUnderAnchorBinder` 链路。主项目默认构建场景改为 `BridgeStereoFusion`，包含原有 Lighter、手势和绑定组件。原 `BridgeTest` 场景保留作回退。

历史版本 1.7.2：首次保持静止完成位置、底部和高度校准，随后等待右手点赞确认召唤，模型才会显示。PC 连接失败后每隔两秒重试；失去头部追踪后重新校准并等待召唤。

## 召唤等待与短冷却（1.7.2）

- 修复手靠近暂停视觉更新、3 秒保护到期后又需要新视觉结果才能召唤的等待冲突。一次成功校准后保留召唤资格，直到 Reset、应用暂停或头部追踪丢失；遮挡、拒绝帧或 PC 暂时断连不会让已有校准失效。首次校准仍需要 PC 和可靠双目结果。
- 使用保存位置时显示 `Using saved pose; moved? Left Reset`。如果真实物体在无有效视觉结果期间移动过，请 Reset 重新定位；保存位置不是实时追踪保证。
- 点赞停留从 0.5 秒缩短到 0.3 秒；允许四指自然弯曲、拇指自然倾斜。召唤后张掌隔离从 0.25 秒缩短到 0.15 秒。
- 桌面变换的识别冷却从 1 秒缩短到 0.35 秒；分发层冷却和变花动作保护也统一为 0.35 秒，避免下层继续拦截。Reset 的独立保护保留。

## 召唤底部与朝向修正（1.7.1）

- 先让打火机完整可见、保持静止，等待 READY 再靠近做点赞。任一手靠近已定位目标时，召唤前也暂停视觉更新。1.7.2 已取消召唤资格的 3 秒超时，保留已校准位置。
- 显示前按当前底部和校准高度重新配准，模型的宽面（本地 +Z）只在召唤时转向用户，保持竖直；之后不会随头部转动持续旋转。此处不是对真实打火机朝向的视觉估计。
- 配准直接变换网格的局部边界，避免隐藏状态下依赖尚未更新的 Renderer 世界边界；保持高度和底部对齐。
- 桌面模式保留初次校准的底部高度。后续检测底部偏差超过 2.5 cm 时拒绝位置更新，其余更新只跟随桌面内的平移。抬起打火机或换到其他高度的桌面需要 Reset 重新定位。
- 验证：从打火机正前方和侧方分别 Reset、定位、点赞；首次显示的宽面应朝向召唤者，底部不应因点赞而上移。再验证水平移动、炭化及变花。日志 `[SummonPose]` 与 `SUMMONED` 记录底部、高度、根节点、朝向和保护状态；`REJECTED_BASE_HEIGHT` 表示底部异常被拒绝。

## 点赞确认召唤与验证（1.7.0）

1. 让直立的真实打火机完整进入视野。出现 `READY - thumbs-up to summon` 后，虚拟模型仍应隐藏。
2. 右手在打火机侧边做点赞：食指、中指、无名指、小指弯曲，拇指伸直朝上，停稳约 0.3 秒。手掌距目标 60 cm 内，让头显看清拇指，不用指向或碰触打火机。显示 `Thumbs-up - hold` 和进度表示手型已识别。
3. 虚拟打火机出现后，按 `Summoned! Open hand to continue` 提示张开手掌约 0.15 秒，再做原有变换。仅把拇指收回变成拳头不会解除这道隔离；张手过程不计入握拳张开变花，也不计入捏合或挥掌。
4. 左手 Reset、应用暂停恢复或头部追踪丢失后，需要重新定位并再次召唤。

召唤检查四指弯曲、拇指两个指节方向较一致、拇指整体朝上（允许约 69 度以内倾斜）且与食指尖分离。手掌偏移超过 4 cm、手部采样中断超过 0.15 秒、手型改变会重新计时；已校准位置不再因视觉结果过期失去召唤资格。日志 `[TabletopThumb]` 包含手型判定、手指伸展比例和进度，供实机调参使用。

## 桌面跟随与验证（1.4.0）

- 保持打火机直立、完整可见，首次静止定位后显示 `FOLLOW`。缓慢移动几厘米，移开手后约 0.8 秒恢复视觉跟随，并平滑更新位置。
- 任一被追踪的手掌、食指尖或拇指尖进入目标 24 cm 内时显示 `HOLD - hand nearby`，暂停定位更新，保留手势动画。期间及恢复前拍摄的图像不能移动锚点。
- 小于 6 mm 的位置变化作为抖动忽略；大于 6 cm 的跳变需要三个相互接近的测量确认。跟随阶段拒绝超过 0.6 秒的结果和高度偏离初始值超过 30% 的识别框。
- 遮挡或断连时保持最后位置，状态显示 HOLD 和原因；恢复可靠识别后继续跟随。此时保持的位置不代表真实物体仍在那里。
- 先验证静止时抖动、移动后重新贴合，再验证炭化、缩放、闪避和变花后移动真实物体；动画不应被识别刷新重置，花应与共同锚点一起移动。左手 Reset 可重新进行初始尺寸和位置校准。
- 本版仅跟随直立物体的位置，不估计旋转，不支持完全遮挡后抓握跟手。手持期间位置暂时保持，不能用于验证连续手持追踪。

## 数据如何流动

1. PICO 左右 RGB 相机采集 640×480 图像，检查左右时间戳差不超过 5 ms。时间戳检查不能单独证明硬件曝光同步。
2. 向 PC 发送左图 JPEG，同时保存对应的左右灰度图、相机参数、拍摄时头部世界姿态及 `frame_id`。一次只等待一张图的回复。
3. PC 使用 `object-detection-demo/weights/best_yolo26n_quad_v4_50e.pt`，返回该图的识别框。没有修改或重新训练权重。
4. 拒绝不匹配的帧号、过期回复以及缺少拍摄姿态的帧。将 JPEG 中的识别框映射回原始相机像素。
5. 在打火机框内部取 3×3 个纹理小块计算视差。框过小、纹理不足或匹配不一致时拒绝定位，避免使用背景或固定距离补值。
6. 使用 `Z = fx × baseline / disparity`，结合像素和相机内参计算相机 XYZ，再用相机外参和拍摄时头部姿态转换为世界 XYZ。
7. 初次稳定定位后锚点接收可靠的新坐标并平滑移动；子模型保留校准尺寸和动画偏移，花同步平移。临时失去视觉结果保留最后位置；失去头部追踪则隐藏并重新定位。

此处的 Z 来自 RGB 双目计算，不是 ToF 原始深度。Spatial Mesh 和 SecureMR 深度均关闭。模型高度和底部由首次稳定识别框估计；旋转及表面采样点与模型中心之间的偏差仍未标定，不能把位置接通等同于完整轮廓对齐。

## 构建与回退

Unity 菜单：`Bridge > Build Stereo YOLO APK`。

输出：`Builds/Android/BridgeStereoFusion.apk`。
应用名：`Magic MR Stereo YOLO`，包名：`com.yn.picmagicmr.stereo`。独立包名可保留旧 APK，构建结束恢复原项目的包名和签名选项。

菜单会从原场景生成双目版本，关闭原单相机采集、Mesh 和旧预览，保留原模型、手势与绑定组件。左侧显示定位状态，中央十字只是视线参考；自动定位依据 YOLO 框，无需按扳机。

回退时打开原 `Assets/Scenes/BridgeTest.unity`，并在 Build Profiles 场景列表中启用它、关闭 `BridgeStereoFusion`。如需重新构建旧 Mesh 版本，恢复 PICO 项目设置中的 Spatial Mesh 开关；新构建菜单会将该全局开关关闭。

## 运行

PC 启动现有 `object-detection-demo/tcp_server.py`；PICO 通过 USB 连接，并配置 `adb reverse tcp:5005 tcp:5005`。先启动 PC 服务，再打开新版应用。

让打火机完整进入摄像头视野。`LIGHTER_LOCATED` 表示本次识别已有可接受的双目结果。`Distance` 是拍摄时头部中心到测量点的直线距离；`Z` 是左相机沿光轴的深度，两者不是同一个数值。`LighterTooSmall` 表示识别框不足以容纳可靠纹理采样；`NoReliableLighterDepth` 表示本次匹配未通过。`PC_DISCONNECTED` 表示检查 TCP Server 或 USB 转发，程序会自动重连。

应用恢复前台时会重建相机会话，并清空之前的帧和姿态缓存。初次验证重点观察静止打火机上模型是否出现、转头后是否保持位置；真实叠加误差仍需在设备上检查。

日志标签：`[StereoCapture]`、`[StereoYOLO]`、`[AnchorManager]`。
